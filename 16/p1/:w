use std::fs;

fn main() {
    let input = fs::read_to_string("test").unwrap();

    let field: Vec<Vec<char>> = input.lines().map(|line| {
        line.chars().collect()
    })
    .collect();

    println!("{:?}", field);
}
